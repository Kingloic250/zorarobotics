{
  "name": "zorabots",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm install && cd frontend && npm install && npm run build && cd ../backend && npm install && cd .. && npx tsc",
    "start": "node dist/backend/server.js",
    "dev": "cd frontend && npm run dev",
    "start:backend": "cd backend && npm run dev"
  },
  "devDependencies": {
    "tsx": "^4.22.4",
    "typescript": "^6.0.3"
  }
}
